IF COMMAND NOT FOUND WHILE CHECKING VERSION DO THIS: 
# Install Vue CLI using npm in bash command run this:
#   npm install -g @vue/cli
# Use pip to install Flask using bash command run this:
#   pip install flask
